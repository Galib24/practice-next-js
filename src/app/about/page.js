import React from 'react';

export const metadata = {
    title: 'About || Next Hero',
    description: 'Abul',
  }

const AboutPage = () => {
    return (
        <div>
            AboutPage
        </div>
    );
};

export default AboutPage;